fastapi==0.104.1 # web framework to build REST APIs
uvicorn[standard]==0.24.0 # the server that runs FastAPI
redis==5.0.1 # Python client to talk to Redis
psycopg2-binary==2.9.9 # Python client to talk to PostgreSQL
sqlalchemy==2.0.23 # ORM, lets you write Python instead of raw SQL
pydantic==2.5.0 # validates data types automatically
pydantic-settings==2.1.0 # reads config from environment variables

